</div>
</div>
  <p id="notice"><%= notice %></p>

<div class="row">
  <div class="col-xs-6 col-md-4">
    <%=@patient.street %><br>
    <%=@patient.city %> <%=@patient.zip %><br>
    <strong><%=@patient.phone %></strong>
  </div>

  <div class="col-xs-6 col-md-4">
    <h2><%=@patient.name %></h2><br>
  </div>

  <div class="col-xs-6 col-md-4">
      Health Notes:<br>
      <%=@patient.condition %>
  </div>
</div>

<div class="row">
  <div class="col-md-4 col-md-offset-1">
    <h6>Check-In Time</h6></div>

  <div class="col-md-5 col-md-offset-2"><h6>Message</h6></div>
  Last Check-In:
  "% @checkins = @checkins.order('created_at DESC') %"
  "% time_diff = Time.now.to_i - @patient.updated_at.to_i %"
  "%= distance_of_time_in_words(time_diff) %"" ago
</div>

<table class="table table-striped table-hover">
  <tr>
    <th>Check-In Date</th>
    <th>Message </th>
  </tr>
  <tr>
    KAYLA-CRAIG CHANGE the "% %" to be erb tags
      "% @checkins.each do |checkin|  %"
    <td>CHANGE <%=Time.now.strftime("%A %m/%d/%Y at %I:%M%p") %> </td>
    <td>CHANGE "%= checkin.message%"</td>
  </tr>
</table>

